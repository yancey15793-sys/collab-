{
  "name": "briefeed",
  "private": true,
  "version": "0.0.0",
  "description": "Briefeed — Personal News Intelligence Engine",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:generate": "npm run db:generate -w @briefeed/db",
    "db:migrate": "npm run db:migrate -w @briefeed/db",
    "dev:api": "npm run dev -w @briefeed/api",
    "dev:worker": "npm run dev -w @briefeed/worker",
    "dev:web": "npm run dev -w @briefeed/web"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "eslint": "^9.14.0",
    "@typescript-eslint/parser": "^8.13.0",
    "@typescript-eslint/eslint-plugin": "^8.13.0",
    "prettier": "^3.3.3"
  }
}
