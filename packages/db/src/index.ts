export * from './schema.js';

// Le client de connexion (drizzle-orm/postgres-js) et les implémentations de
// repositories (DrizzleStoryRepository, etc.) sont ajoutés en Phase 1, une fois
// la stratégie de base de données de dev confirmée (voir docs/decisions/0007-*.md).
